
/**
 * A service for compiling the form required for either Practioner or User 
 */
 class FormService  {
    /**
     * Compiles the forms required from the form package 
     * @returns The required forms for the users  
     */
    static fetchForm = async () => {
      
    };
  }
  
  export default formService;
  