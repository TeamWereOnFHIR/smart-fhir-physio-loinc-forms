// Placeholder, common/theme folder would be a good place to do global themeing.
// We can use normal CSS, but other modules exist to do global styling as well for react.

export const dummy = () => undefined;
